----------------------PART 1--------------------------------------------
--there are 4 different types of JOIN clauses in SQL: 
--INNER, LEFT, RIGHT,  and FULL.
-- As a special case, there may be a CROSS JOIN, which is a table 
--(base table, view, or joined table) can JOIN to itself in a self-join. 
--A programmer declares a JOIN statement to identify rows for joining.
----------------------PART 2--------------------------------------------

----The JOIN used to create the final view was a left join. 

